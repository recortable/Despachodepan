<% title 'selección' %>

<div id="wrapper">
  <div id="time-panel" style="width: <%= @selection.width %>px;">
    <div class="line">
        <%= render partial: 'selection', collection: @selection.top %>
    </div>
    <div class="line">
        <%= render partial: 'selection', collection: @selection.bottom %>
    </div>
  </div>
</div>
